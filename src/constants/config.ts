import { expo } from '@/../app.json';

/**
 * app related variables
 */
export const APP_NAME = expo.name;
export const APP_SLUG = expo.slug;
export const SESSION_TOKEN = `${APP_NAME}_SESSION_TOKEN`;
export const USER_SET_DARK_MODE = `${APP_NAME}_USER_SET_DARK_MODE`;
