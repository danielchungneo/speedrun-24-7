import { APP_SLUG } from './config';

export const TOKEN_STORAGE_KEY = `${APP_SLUG}-auth-token`;
export const USER_STORAGE_KEY = `${APP_SLUG}-user`;
export const USERNAME_STORAGE_KEY = `${APP_SLUG}-username`;
export const PASSWORD_STORAGE_KEY = `${APP_SLUG}-password`;

